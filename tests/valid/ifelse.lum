// If-Else condition test
int main() {
    int score;
    score = 85;

    if (score >= 90) {
        print(1);
    } else {
        if (score >= 80) {
            print(2);
        } else {
            print(3);
        }
    }
    return 0;
}
